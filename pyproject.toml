[project]
name = "monobit-py"
version = "0.1.0"
description = "Monobit Python SDK"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "requests>=2.32.5",
]

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"